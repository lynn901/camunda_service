import { PageContainer } from '@ant-design/pro-components';
import { Card } from 'antd';
import React from 'react';

const Org: React.FC = () => {
  return (
    <PageContainer>
      <Card>
        组织与权限管理 (待开发)
      </Card>
    </PageContainer>
  );
};

export default Org;
